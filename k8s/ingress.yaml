apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dbdocs-viewer
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: dbdocs.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dbdocs-viewer
                port:
                  number: 80
